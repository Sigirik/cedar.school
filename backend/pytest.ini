[pytest]
DJANGO_SETTINGS_MODULE = config.settings
python_files = tests.py test_*.py *_tests.py
addopts = -q
markers =
    collisions: schedule collision rules
    pipeline: real-schedule generation pipeline
    api: API-level tests
    slow: performance or heavy tests