server {
  listen 80;
  server_name dev.cedar.school;
  location /.well-known/acme-challenge/ { root /var/www/letsencrypt; }
  location / { return 301 https://$host$request_uri; }
}

server {
  listen 443 ssl http2;
  server_name dev.cedar.school;

  ssl_certificate     /etc/letsencrypt/live/dev.cedar.cedar.school/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/dev.cedar.cedar.school/privkey.pem;

  add_header Strict-Transport-Security "max-age=15552000" always;

  location / {
    proxy_pass http://127.0.0.1:5001;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
  }

  access_log /var/log/nginx/dev.access.log;
  error_log  /var/log/nginx/dev.error.log warn;
}
