server {
  listen 80;
  server_name api.cedar.school;
  location /.well-known/acme-challenge/ { root /var/www/letsencrypt; }
  location / { return 301 https://$host$request_uri; }
}

server {
  listen 443 ssl http2;
  server_name api.cedar.school;

  ssl_certificate     /etc/letsencrypt/live/api.cedar.school/fullchain.pem;
  ssl_certificate_key /etc/letsencrypt/live/api.cedar.school/privkey.pem;

  add_header Strict-Transport-Security "max-age=15552000" always;
  client_max_body_size 20m;

  location / {
    proxy_pass http://127.0.0.1:5301;
    proxy_set_header Host $host;
    proxy_set_header X-Real-IP $remote_addr;
    proxy_set_header X-Forwarded-For $proxy_add_x_forwarded_for;
    proxy_set_header X-Forwarded-Proto $scheme;
    proxy_read_timeout 60s;
    proxy_send_timeout 60s;
  }

  access_log /var/log/nginx/api-prod.access.log;
  error_log  /var/log/nginx/api-prod.error.log warn;
}
